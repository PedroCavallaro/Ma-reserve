import RegisterForm from "../components/Register/RegisterForm";

export default function Register() {
    return (
        <main className="bg-orange-400 w-full h-screen flex justify-center items-center">
            <RegisterForm />
        </main>
    );
}
