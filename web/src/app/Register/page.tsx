import RegisterForm from "@/components/Register/RegisterForm";

export default function Register() {
    return(
        <main className="h-screen fixed w-screen container flex items-center justify-center bg-orange-orangePrimary">
            <RegisterForm/>
        </main>
    )
};
